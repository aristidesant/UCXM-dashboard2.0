Underline-style tabs for switching between two or three views within a screen, active tab underlined in interactive blue.

```jsx
<TopTabs tabs={["Indicadores", "Detalles de contacto"]} activeIndex={0} onChange={setTab} />
```

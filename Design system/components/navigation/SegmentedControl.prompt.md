Pill segmented control for switching between two or three sibling views, distinct from `TopTabs` (underline style) — this one lifts the active option on a white pill inside a gray track.

```jsx
<SegmentedControl options={["Reporte", "Contactos"]} activeIndex={1} onChange={setView} />
```

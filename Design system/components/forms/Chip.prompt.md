Pill filter chip. Active chip gets a green tint and an optional leading icon (checkmark, etc).

```jsx
<Chip label="Todos (15)" active icon={<i data-lucide="check-circle" />} />
<Chip label="Efectivo (11)" onClick={select} />
```
